# SPDX-License-Identifier: GPL-2.0
#
# Compat 模块 - 仅保留辅助函数
#
# 注意：所有外设接口（GPIO, PWM, I2C, SPI, CAN, Serial）已删除
# 原因：无任何代码使用这些接口
#
# 保留的辅助函数（头文件形式）：
# - pdm_compat_sysfs.h: sysfs_emit 兼容函数
# - pdm_compat_features.h: 内核特性检测
# - pdm_compat_errno.h: 错误码转换

# 无需编译任何 .c 文件
