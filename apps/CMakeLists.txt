if(CONFIG_DEBUG_STUDY)
    add_subdirectory(debug_study)
endif()

if(CONFIG_SMART_MONITOR)
    add_subdirectory(smart_monitor)
endif()

if(CONFIG_MEMORY_MONITOR)
    add_subdirectory(memory_monitor)
endif()
